"""Placeholder for region_mask module."""
from __future__ import annotations

import numpy as np


def apply_region_mask(frame: np.ndarray) -> np.ndarray:
    """Placeholder: returns an empty mask."""
    _ = frame
    return np.zeros((), dtype=np.uint8)
